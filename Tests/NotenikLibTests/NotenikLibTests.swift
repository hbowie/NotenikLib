import XCTest
@testable import NotenikLib

final class NotenikLibTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        // XCTAssertEqual(NotenikLib().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
